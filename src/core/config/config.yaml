# Sample config. Copy to config.local.yaml and replace CHANGE_ME values.
credentials:
  usernames:
    demo:
      email: demo@example.com
      name: Demo User
      password: CHANGE_ME

cookie:
  expiry_days: 30
  key: "CHANGE_ME"
  name: "dlc_webui_cookie"

preauthorized:
  emails: []
